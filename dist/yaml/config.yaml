year: integer(*)[年份]<2015>/20[0-9]{2}/
date: time(*)
members: 
  - name: string
    begin: 
      - thread: string
        items: 
          - desc: text
            priority: integer
            willbedone: boolean
            timealloc: integer
    end:
      - thread: string
        items:
          - desc: string
            isdone: boolean
            timeused: integer
            submits:
              - name: string
                link: string
                comment: text
                note: text
